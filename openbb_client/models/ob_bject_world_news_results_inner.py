# coding: utf-8

"""
    OpenBB Platform API

    Investment research for everyone, anywhere.

    The version of the OpenAPI document: 1
    Contact: hello@openbb.co
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from openbb_client.models.benzinga_world_news_data import BenzingaWorldNewsData
from openbb_client.models.fmp_world_news_data import FMPWorldNewsData
from openbb_client.models.intrinio_world_news_data import IntrinioWorldNewsData
from openbb_client.models.tiingo_world_news_data import TiingoWorldNewsData
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

OBBJECTWORLDNEWSRESULTSINNER_ONE_OF_SCHEMAS = ["BenzingaWorldNewsData", "FMPWorldNewsData", "IntrinioWorldNewsData", "TiingoWorldNewsData"]

class OBBjectWorldNewsResultsInner(BaseModel):
    """
    OBBjectWorldNewsResultsInner
    """
    # data type: TiingoWorldNewsData
    oneof_schema_1_validator: Optional[TiingoWorldNewsData] = None
    # data type: BenzingaWorldNewsData
    oneof_schema_2_validator: Optional[BenzingaWorldNewsData] = None
    # data type: FMPWorldNewsData
    oneof_schema_3_validator: Optional[FMPWorldNewsData] = None
    # data type: IntrinioWorldNewsData
    oneof_schema_4_validator: Optional[IntrinioWorldNewsData] = None
    actual_instance: Optional[Union[BenzingaWorldNewsData, FMPWorldNewsData, IntrinioWorldNewsData, TiingoWorldNewsData]] = None
    one_of_schemas: Set[str] = { "BenzingaWorldNewsData", "FMPWorldNewsData", "IntrinioWorldNewsData", "TiingoWorldNewsData" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = OBBjectWorldNewsResultsInner.model_construct()
        error_messages = []
        match = 0
        # validate data type: TiingoWorldNewsData
        if not isinstance(v, TiingoWorldNewsData):
            error_messages.append(f"Error! Input type `{type(v)}` is not `TiingoWorldNewsData`")
        else:
            match += 1
        # validate data type: BenzingaWorldNewsData
        if not isinstance(v, BenzingaWorldNewsData):
            error_messages.append(f"Error! Input type `{type(v)}` is not `BenzingaWorldNewsData`")
        else:
            match += 1
        # validate data type: FMPWorldNewsData
        if not isinstance(v, FMPWorldNewsData):
            error_messages.append(f"Error! Input type `{type(v)}` is not `FMPWorldNewsData`")
        else:
            match += 1
        # validate data type: IntrinioWorldNewsData
        if not isinstance(v, IntrinioWorldNewsData):
            error_messages.append(f"Error! Input type `{type(v)}` is not `IntrinioWorldNewsData`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in OBBjectWorldNewsResultsInner with oneOf schemas: BenzingaWorldNewsData, FMPWorldNewsData, IntrinioWorldNewsData, TiingoWorldNewsData. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in OBBjectWorldNewsResultsInner with oneOf schemas: BenzingaWorldNewsData, FMPWorldNewsData, IntrinioWorldNewsData, TiingoWorldNewsData. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into TiingoWorldNewsData
        try:
            instance.actual_instance = TiingoWorldNewsData.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into BenzingaWorldNewsData
        try:
            instance.actual_instance = BenzingaWorldNewsData.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into FMPWorldNewsData
        try:
            instance.actual_instance = FMPWorldNewsData.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into IntrinioWorldNewsData
        try:
            instance.actual_instance = IntrinioWorldNewsData.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into OBBjectWorldNewsResultsInner with oneOf schemas: BenzingaWorldNewsData, FMPWorldNewsData, IntrinioWorldNewsData, TiingoWorldNewsData. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into OBBjectWorldNewsResultsInner with oneOf schemas: BenzingaWorldNewsData, FMPWorldNewsData, IntrinioWorldNewsData, TiingoWorldNewsData. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], BenzingaWorldNewsData, FMPWorldNewsData, IntrinioWorldNewsData, TiingoWorldNewsData]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


